/* class User {
  constructor(firstName, lastName, age, location) {
    this.firstName = firstName;
    this.lastName = lastName;
    this.age = age;
    this.location = location;
  }
  confronto(other) {
    if (this.age > other.age) {
      console.log(`${this.firstName} è più grande di ${other.firstName}`);
    } else if (this.age === other.age) {
      console.log(`${this.firstName} è uguale a ${other.firstName}`);
    } else {
      console.log(`${this.firstName} è più piccolo di ${other.firstName}`);
    }
  }
}

const utentex = new User("valentino", "rossi", 45, "monza");
const utentey = new User("Giovanni", "D'Anna", 26, "Cirignano");

utentex.confronto(utentey); */
